// ─────────────────────────────────────────────────────────────
// CameraRig.tsx — Milestone 2
//
// Stub: The GSAP-driven camera state machine.
// Implements: IDLE → TRANSITIONING → IDLE with interruption support.
// Reads activeSection from Zustand cameraSlice.
// Writes transitionStatus back to the store on completion.
// ─────────────────────────────────────────────────────────────

// Full implementation in M2.
// Depends on: gsap, @react-three/fiber useFrame, config/cameraStates
export default function CameraRig() {
  return null;
}
